import 'package:flutter/material.dart';

class EsportOverviewView extends StatelessWidget {
  const EsportOverviewView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Overview'),
    );
  }
}
